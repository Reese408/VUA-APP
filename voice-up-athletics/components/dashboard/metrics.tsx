//this will be for only specific accounts such as admin accounts to see metrics about user engagement, mental health improvement stats, etc.

export default function DashboardMetrics(){
    return(
        <section>
            
        </section>
    );
}