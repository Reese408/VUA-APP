'use client';

//this is the main page component for the dashboard that will hold all other dashboard components such as metrics, graphs, user info, etc.
//this should be dynamic and update based on the users role and permissions

export default function MainPage(){
    return(
        <section>
        </section>
    );
}   